# This config was automatically generated from your source code
# Stacks detected: cicd:jenkins:.,deps:python:.,file:manage.py:.
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-python:
    # Install dependencies and run tests
    docker:
      - image: cimg/python:3.8-node
    steps:
      - checkout
      - python/install-packages
      - run:
          name: Run tests
          command: python manage.py test
  buildandpush:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: buildandpush
          command: docker build -t Djangoapp .
  push:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: push
          command: docker push amitsingh01/Djangoapp:latestci

workflows:
  build-and-test:
    jobs:
      - test-python
      - buildandpush
      - push
    # - deploy:
    #     requires:
    #       - test-python
